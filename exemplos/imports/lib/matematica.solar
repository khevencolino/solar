// Módulo de funções matemáticas customizadas
// Para testar imports relativos

definir dobro(x: inteiro): inteiro {
    retornar x * 2
}

definir triplo(x: inteiro): inteiro {
    retornar x * 3
}

definir quadrado(x: inteiro): inteiro {
    retornar x * x
}
